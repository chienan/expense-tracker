<div class="container">
  <div class="row my-auto justify-content-center align-items-center text-center">
    <form action="/records" method="POST">

      <div class="form-group">
        <select class="form-control" id="filter-category" name="category" onchange="submit">

          <option disabled selected>支出類別</option>
          {{#each categories}}
          <button name="filterCategory" value="{{ this.name }}" class="dropdown-item">{{ this.name }}</button>
          <option value="{{this.name}}">{{this.name}}</option>
          {{/each}}
        </select>
      </div>

      <input type="text" placeholder="支出名稱" name="name" class="mb-2 form-control" style="width: 300px;" required>

      <input type="date" placeholder="日期" name="date" class="mb-2 form-control" style="width: 300px;" required>

      <input type="number" placeholder="金額" name="amount" class="mb-2 form-control" style="width: 300px;" required>


      <button type="submit" class="btn btn-outline-secondary">新增</button>

    </form>
  </div>
</div>